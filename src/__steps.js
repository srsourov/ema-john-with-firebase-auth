/*
    1. Create a context provider file (ex: AuthProvider.jsx)
    2. create a context (ex: "const AuthContext = createContext(null)") and set provider (ex. "<AuthContext.Provider>")
    3. set the children props (ex. {children})
    4. set context value (ex. "value={}"")
    5. set provider
*/



/*
HOSTING
---------------------

One time per pc
    1. npm install -g firebase-tools
    2. firebase login

For each project one time

    1. firebase init
    2. proceed
    3. hosting: firebase (up and down arrow) use spacebar
    4. existing project
    5. select the project carefully
    6. which project as public directory: dist
    7. single page application: yes
    8. continous deployment: no

For everytime run build

    1. npm run build
    2. firebase deploy



*/